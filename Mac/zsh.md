```shell
# 查看zsh启动时间
/usr/bin/time /bin/zsh -t -c exit

# 切换到bash
chsh -s /bin/bash
```

```shell
# 安装 ohmyzsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# 配置
# 主题：steeef
# 插件 git z
```

